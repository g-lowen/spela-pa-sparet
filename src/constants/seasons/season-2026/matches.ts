import { Match } from "../../../types";
import { Season2026Team } from "./teams";

/**
 * Twelve group matches, then the semifinal and the final, in episode order.
 * Set `winner` to null until an episode has aired.
 */
export const MATCHES: Match<Season2026Team>[] = [];
